Flask==3.1.3

# Realtime (этап 5): WebSocket-транспорт ходов. async_mode="threading" —
# eventlet/gevent НЕ нужны. simple-websocket даёт Werkzeug dev-серверу настоящий
# WebSocket (иначе деградация на long-polling).
Flask-SocketIO>=5.4
simple-websocket>=1.0

# Слой данных (этап 2): SQLAlchemy 2.0 + psycopg 3, миграции — Alembic
SQLAlchemy>=2.0.36
alembic>=1.13
psycopg[binary]>=3.2
python-dotenv>=1.0

pytest>=8.2
